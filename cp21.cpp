#include <iostream>

using namespace std;

int main()
{
    int n, x, y, num = 1;
    cout << "Input the number of rows : ";
    cin >> n;

    for (x = 1; x <= n; x++)
    {
        for (y = 1; y <= n; n um++, y++)
            cout << num;
        cout << "\n";
    }

    return 0;
}